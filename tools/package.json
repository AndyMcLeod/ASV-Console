{
  "private": true,
  "description": "Doc tooling deps for the generated document set (node build_docs.js) and the presentation (node build_deck.js). Run: npm install first.",
  "dependencies": {
    "docx": "^9.7.1",
    "pptxgenjs": "^4.0.1"
  }
}
